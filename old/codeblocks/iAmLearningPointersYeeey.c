#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main() {
    int length;
    int i;
    char temp;
    char *array;

    printf("Please enter a string: ");
    scanf(" %s", array);

    printf("\n%s", array);

    array = (int *) malloc(strlen(temp) * sizeof(char));







    return 0;
}
